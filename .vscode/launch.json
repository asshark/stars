{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch stars webpage",
            "url": "http://localhost:7080",
            "webRoot": "${workspaceRoot}"
        }
    ]
}